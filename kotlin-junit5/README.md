A Kotlin version of name-normalizer that uses JUnit 5 and AssertJ.

This is a Gradle build (Kotlin DSL). 

An example solution is in solution-kotlin-junit5 as a series of commits.

Another example solution, with detailed notes on each step, is in solution-kotlin-junit5-annotated,
as a series of commits.